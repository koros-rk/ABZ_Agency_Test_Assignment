// eslint-disable-next-line no-shadow
export enum ErrorMessages {
  invalidRatio = 'Invalid image ratio, minimum ratio of photo is 70x70px',
  invalidSize = 'File is too heavy, size must not be greater than 5 Mb',
  invalidFormat = 'Wrong file format, only jpg/jpeg allowed'
}
